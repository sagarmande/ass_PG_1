Sure, here are Python programs for each of the tasks you've mentioned:

1. Print "Hello Python":
```python
print("Hello Python")
```

2. Perform addition and division:
```python
# Addition
num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))
sum_result = num1 + num2
print("Sum:", sum_result)

# Division
numerator = float(input("Enter numerator: "))
denominator = float(input("Enter denominator: "))
if denominator != 0:
    division_result = numerator / denominator
    print("Division result:", division_result)
else:
    print("Division by zero is not allowed.")
```

3. Calculate the area of a triangle:
```python
base = float(input("Enter the base length of the triangle: "))
height = float(input("Enter the height of the triangle: "))

area = 0.5 * base * height
print("The area of the triangle is:", area)
```

4. Swap two variables:
```python
# Input values
x = input("Enter the value of x: ")
y = input("Enter the value of y: ")

# Swapping using a temporary variable
temp = x
x = y
y = temp

# Output swapped values
print("After swapping:")
print("x =", x)
print("y =", y)
```

5. Generate a random number:
```python
import random

# Generate a random integer between 1 and 100
random_number = random.randint(1, 100)
print("Random number:", random_number)
```

These are simple Python programs for the tasks you specified. You can run each of them in a Python environment to see the results.
